/**
 * mailcow API
 * mailcow is complete e-mailing solution with advanced antispam, antivirus, nice UI and API.  In order to use this API you have to create a API key and add your IP address to the whitelist of allowed IPs this can be done by logging into the Mailcow UI using your admin account, then go to Configuration > Access > Edit administrator details > API. There you will find a collapsed API menu.  There are two types of API keys   - The read only key can only be used for all get endpoints   - The read write key can be used for all endpoints
 *
 * The version of the OpenAPI document: 1.0.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The CreateTLSPolicyMapRequest model module.
 * @module model/CreateTLSPolicyMapRequest
 * @version 1.0.0
 */
class CreateTLSPolicyMapRequest {
    /**
     * Constructs a new <code>CreateTLSPolicyMapRequest</code>.
     * @alias module:model/CreateTLSPolicyMapRequest
     */
    constructor() { 
        
        CreateTLSPolicyMapRequest.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>CreateTLSPolicyMapRequest</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/CreateTLSPolicyMapRequest} obj Optional instance to populate.
     * @return {module:model/CreateTLSPolicyMapRequest} The populated <code>CreateTLSPolicyMapRequest</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new CreateTLSPolicyMapRequest();

            if (data.hasOwnProperty('parameters')) {
                obj['parameters'] = ApiClient.convertToType(data['parameters'], 'String');
            }
            if (data.hasOwnProperty('active')) {
                obj['active'] = ApiClient.convertToType(data['active'], 'Number');
            }
            if (data.hasOwnProperty('dest')) {
                obj['dest'] = ApiClient.convertToType(data['dest'], 'String');
            }
            if (data.hasOwnProperty('policy')) {
                obj['policy'] = ApiClient.convertToType(data['policy'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>CreateTLSPolicyMapRequest</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>CreateTLSPolicyMapRequest</code>.
     */
    static validateJSON(data) {
        // ensure the json data is a string
        if (data['parameters'] && !(typeof data['parameters'] === 'string' || data['parameters'] instanceof String)) {
            throw new Error("Expected the field `parameters` to be a primitive type in the JSON string but got " + data['parameters']);
        }
        // ensure the json data is a string
        if (data['dest'] && !(typeof data['dest'] === 'string' || data['dest'] instanceof String)) {
            throw new Error("Expected the field `dest` to be a primitive type in the JSON string but got " + data['dest']);
        }
        // ensure the json data is a string
        if (data['policy'] && !(typeof data['policy'] === 'string' || data['policy'] instanceof String)) {
            throw new Error("Expected the field `policy` to be a primitive type in the JSON string but got " + data['policy']);
        }

        return true;
    }


}



/**
 * custom parameters you find out more about them [here](http://www.postfix.org/postconf.5.html#smtp_tls_policy_maps)
 * @member {String} parameters
 */
CreateTLSPolicyMapRequest.prototype['parameters'] = undefined;

/**
 * 1 for a active user account 0 for a disabled user account
 * @member {Number} active
 */
CreateTLSPolicyMapRequest.prototype['active'] = undefined;

/**
 * the target domain or email address
 * @member {String} dest
 */
CreateTLSPolicyMapRequest.prototype['dest'] = undefined;

/**
 * the policy
 * @member {module:model/CreateTLSPolicyMapRequest.PolicyEnum} policy
 */
CreateTLSPolicyMapRequest.prototype['policy'] = undefined;





/**
 * Allowed values for the <code>policy</code> property.
 * @enum {String}
 * @readonly
 */
CreateTLSPolicyMapRequest['PolicyEnum'] = {

    /**
     * value: "none"
     * @const
     */
    "none": "none",

    /**
     * value: "may"
     * @const
     */
    "may": "may",

    /**
     * value: "encrypt"
     * @const
     */
    "encrypt": "encrypt",

    /**
     * value: "dane"
     * @const
     */
    "dane": "dane",

    /**
     * value: "'dane"
     * @const
     */
    "&#39;dane": "'dane",

    /**
     * value: "fingerprint"
     * @const
     */
    "fingerprint": "fingerprint",

    /**
     * value: "verify"
     * @const
     */
    "verify": "verify",

    /**
     * value: "secure"
     * @const
     */
    "secure": "secure"
};



export default CreateTLSPolicyMapRequest;

